package se.ltu.thesis.haidar.agent;


public class Explain {
/*
	private void v(){
		
		double step = 0.1;
		
		for (double mEpsilon = 0; mEpsilon <= 1; mEpsilon += step) {
			for (double mLearningRate = 0; mLearningRate <= 1; mLearningRate += step) {
				for (double mDiscountFactor = 0; mDiscountFactor <= 1; mDiscountFactor += step) {
		
					// setting the agent's
					Agent mAgent = new QleaningAgent(mEpsilon, mLearningRate, mDiscountFactor);
					
					
					double[] mReward = new double[10];
					
					//running 10 learning episods
					for(int i = 0; i < 10; i++){
						mAgent.runLeaningEpisode();
						mReward[i] = mAgent.getCumelativeReward();
					}
					PushToDatabase(mEpsilon, mLearningRate, mDiscountFactor, mReward);
				}// end of the inside loop (new tuple will be set)
			}
		}
	}
}
*/
}
